$(document).ready(function() {
    // various styling things
    //var w = Math.max($(window).width()/2, 300);
    //$('#knitrNotebook').width(w - 10);
    //$('#knitrOut').css('left', w + 10 + 'px');

    // FIXME why doesn't this work?
    $('#knitrRefresh button').click();
})
